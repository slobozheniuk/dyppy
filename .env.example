# Supabase PostgreSQL Connection
# ═══════════════════════════════════════════════════════════════════════════════
#
# Get these values from your Supabase project:
#   Dashboard → Settings → Database → Connection string
#
# DATABASE_URL: Uses the connection pooler (port 6543) via PgBouncer.
#   Used by Prisma Client at runtime for queries.
#
# DIRECT_URL: Direct connection (port 5432), bypasses the pooler.
#   Used by Prisma Migrate / db push for schema changes.
#
# ═══════════════════════════════════════════════════════════════════════════════

# Connect to Supabase via connection pooling
DATABASE_URL="postgresql://postgres.mewvnjkhvcuxvvxaksrx:cAbjup-xavgo1-bynjiz@aws-1-eu-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Direct connection to the database. Used for migrations
DIRECT_URL="postgresql://postgres.mewvnjkhvcuxvvxaksrx:cAbjup-xavgo1-bynjiz@aws-1-eu-west-1.pooler.supabase.com:5432/postgres"